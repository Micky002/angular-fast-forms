import { InjectionToken } from '@angular/core';
import { ControlId } from '../../control';

export const CONTROL_PROPERTIES = new InjectionToken<any>('AFF_CP');
export const CONTROL_ID = new InjectionToken<ControlId>('AFF_C_ID');
