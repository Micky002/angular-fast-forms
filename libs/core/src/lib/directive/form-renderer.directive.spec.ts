import { FormRendererDirective } from './form-renderer.directive';

describe('FormRendererDirective', () => {
  it('should create an instance', () => {
    // const directive = new FormRendererDirective();
    // expect(directive).toBeTruthy();
  });
});
